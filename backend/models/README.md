# Model artifact

`house_price.pkl` is a development-only model that lets the API and browser flow run immediately. It is not used for the report or submission metrics.

After downloading the required Kaggle dataset to `notebooks/data/house_prices.csv`, run `python notebooks/train_house_price_model.py`. That replaces both this file's model and `locations.json` with the real trained pipeline.
